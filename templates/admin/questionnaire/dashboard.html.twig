<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Admin Dashboard - UniMind</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar-brand {
            font-weight: bold;
        }
        .card {
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
        .card-header {
            border-radius: 10px 10px 0 0 !important;
        }
        .btn {
            border-radius: 6px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('admin_dashboard') }}">📊 UniMind Admin</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('admin_dashboard') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_questionnaire_index') }}">Questionnaires</a>
                    </li>
                </ul>
                <span class="navbar-text text-light">
                    Connecté en tant qu'administrateur
                </span>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="mb-4">🏠 Tableau de Bord Administrateur</h1>
                
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
                
                <div class="alert alert-info">
                    <h5 class="mb-2">Bienvenue dans l'interface d'administration UniMind</h5>
                    <p class="mb-0">Gérez vos questionnaires et surveillez les réponses des étudiants.</p>
                </div>
                
                <div class="row mt-4">
                    <div class="col-md-6">
                        <div class="card border-primary">
                            <div class="card-header bg-primary text-white">
                                <h5 class="mb-0">📋 Gestion des Questionnaires</h5>
                            </div>
                            <div class="card-body">
                                <p>Créez, modifiez et supprimez vos questionnaires d'évaluation.</p>
                                <div class="d-grid gap-2">
                                    <a href="{{ path('admin_questionnaire_index') }}" class="btn btn-outline-primary">
                                        📄 Voir tous les questionnaires
                                    </a>
                                    <a href="{{ path('admin_questionnaire_new') }}" class="btn btn-primary">
                                        ➕ Créer un nouveau questionnaire
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="card border-success">
                            <div class="card-header bg-success text-white">
                                <h5 class="mb-0">🚀 Démarrage rapide</h5>
                            </div>
                            <div class="card-body">
                                <h6>Accès direct :</h6>
                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item">
                                        <a href="{{ path('admin_questionnaire_index') }}">/admin/questionnaire</a> - Liste
                                    </li>
                                    <li class="list-group-item">
                                        <a href="{{ path('admin_questionnaire_new') }}">/admin/questionnaire/new</a> - Création
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="mb-0">📝 Prochaines étapes</h5>
                    </div>
                    <div class="card-body">
                        <ol>
                            <li><strong>Créez votre premier questionnaire</strong> en cliquant sur le bouton ci-dessus</li>
                            <li><strong>Ajoutez des questions</strong> au questionnaire</li>
                            <li><strong>Partagez le lien</strong> avec les étudiants</li>
                            <li><strong>Consultez les résultats</strong> et statistiques</li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="mt-5 py-3 bg-light border-top">
        <div class="container text-center text-muted">
            <small>UniMind Admin Panel © {{ "now"|date("Y") }}</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-dismiss alerts after 5 seconds
        setTimeout(function() {
            var alerts = document.querySelectorAll('.alert');
            alerts.forEach(function(alert) {
                var bsAlert = new bootstrap.Alert(alert);
                bsAlert.close();
            });
        }, 5000);
    </script>
</body>
</html>