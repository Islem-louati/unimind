<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Mailjet - UniMind</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f4f6f9;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 32px;
            font-weight: 700;
        }
        .content {
            padding: 40px 30px;
        }
        .success-box {
            background: #d4edda;
            border-left: 4px solid #28a745;
            padding: 20px;
            margin: 20px 0;
            border-radius: 8px;
            color: #155724;
        }
        .info-box {
            background: #e7f3ff;
            border-left: 4px solid #2196f3;
            padding: 20px;
            margin: 20px 0;
            border-radius: 8px;
        }
        .footer {
            background: #f8f9fc;
            padding: 30px;
            text-align: center;
            color: #8898aa;
            font-size: 14px;
            border-top: 1px solid #e9ecef;
        }
        .detail {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            font-family: monospace;
            font-size: 14px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔧 UniMind</h1>
            <p>Test de configuration Mailjet</p>
        </div>
        
        <div class="content">
            <div class="success-box">
                <h2 style="margin-top: 0;">✅ Email envoyé avec succès !</h2>
                <p style="margin-bottom: 0;">Ceci est un email de test envoyé via Mailjet.</p>
            </div>
            
            <h3>📋 Détails du test</h3>
            
            <div class="detail">
                <p><strong>Date d'envoi :</strong> {{ test_date|date('d/m/Y H:i:s') }}</p>
                <p><strong>Expéditeur :</strong> {{ mailer_from }}</p>
                <p><strong>Configuration :</strong> Mailjet API</p>
                <p><strong>Statut :</strong> Test réussi ✅</p>
            </div>
            
            <div class="info-box">
                <h4 style="margin-top: 0;">🔍 Prochaines étapes</h4>
                <ol>
                    <li>Vérifiez que cet email est bien arrivé dans votre boîte de réception</li>
                    <li>Vérifiez vos SPAM si vous ne le voyez pas</li>
                    <li>Ajoutez l'expéditeur à vos contacts</li>
                    <li>Vérifiez les statistiques sur Mailjet</li>
                </ol>
            </div>
            
            <p style="text-align: center; margin-top: 30px;">
                <strong>L'équipe UniMind</strong>
            </p>
        </div>
        
        <div class="footer">
            <p>
                <strong>UniMind</strong> - Plateforme de soutien psychologique<br>
                <a href="mailto:{{ mailer_from }}">{{ mailer_from }}</a>
            </p>
            <p style="margin-top: 10px; font-size: 12px;">
                Cet email a été envoyé automatiquement pour tester la configuration.
            </p>
        </div>
    </div>
</body>
</html>