<form method="post" action="{{ path('app_commentaire_delete', {'commentaire_id': commentaire.commentaire_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.commentaire_id) }}">
    <button class="btn">Delete</button>
</form>
